on:
  push:
   branches:
      - master
name: Deploy website on push
jobs:
  Github-Acition-test:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    
    - name: Sync files
      uses: prem-modi-website/Github-Acition-test@v4.3.2
